{
  "compilerOptions": {
    // --- Opciones de Entorno y Módulos ---
    "target": "ESNext",
    "module": "NodeNext",
    "moduleResolution": "NodeNext", // Clave: Habilita la estrategia moderna de resolución de módulos.
    "baseUrl": "./",              // Clave: Establece la carpeta raíz del proyecto.
    "outDir": "./build",

    // --- Interoperabilidad y Consistencia ---
    "esModuleInterop": true,
    "forceConsistentCasingInFileNames": true, // Importante para compatibilidad entre Windows y Linux.

    // --- Verificación de Tipos y Calidad de Código ---
    "strict": true,
    "skipLibCheck": true
  },
  // --- Archivos a Incluir y Excluir ---
  "include": [
    "src/**/*" // Clave: Le dice a TypeScript que compile TODOS los archivos dentro de la carpeta 'src'.
  ],
  "exclude": [
    "node_modules", // Es buena práctica excluir siempre node_modules.
    "build"         // También la carpeta de salida.
  ]
}